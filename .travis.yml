language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "iplanetcn"
  - git config user.email "iplanetcn@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GITHUB_TOKEN}@${GITHUB_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - source
env:
 global:
   - GITHUB_REF: github.com/iplanetcn/iplanetcn.github.io.git